#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"#!/bin/bash

set -eu

SCRIPTPATH="$( cd "$(dirname "$0")" >/dev/null 2>&1 ; pwd )"
cd "$SCRIPTPATH/.."

docker exec  -it bcn_postgres /bin/bash -c "PGPASSWORD=Bcn@1234 psql -U bcn -d bcn < /var/lib/bcn/postgresql/db_data/copy_commands.sql"